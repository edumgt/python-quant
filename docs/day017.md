# Day 017 — 시계열 데이터 전처리

> **모듈 4: 시계열 데이터 분석** | 2/8일차 | 📈 | 학습시간: 8시간



---

## 오늘 배울 것 (아주 쉽게)

- 날짜 인덱스 설정 및 리샘플링 (resample)
- 결측치 처리: ffill, bfill, 보간법
- 이상값(Outlier) 탐지 및 처리
- 로그 변환과 차분(Differencing)
- 실습: 일별/주별/월별 OHLCV 데이터 전처리

---



### 1. 날짜 인덱스 설정 및 리샘플링 (resample)

**쉽게 이해하기**
- **날짜 인덱스 설정 및 리샘플링 (resample)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 날짜 인덱스 설정 및 리샘플링 (resample) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 결측치 처리: ffill, bfill, 보간법

**쉽게 이해하기**
- **결측치 처리: ffill, bfill, 보간법** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 결측치 처리: ffill, bfill, 보간법 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 이상값(Outlier) 탐지 및 처리

**쉽게 이해하기**
- **이상값(Outlier) 탐지 및 처리** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 이상값(Outlier) 탐지 및 처리 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 로그 변환과 차분(Differencing)

**쉽게 이해하기**
- **로그 변환과 차분(Differencing)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 로그 변환과 차분(Differencing) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 실습: 일별/주별/월별 OHLCV 데이터 전처리

**쉽게 이해하기**
- **실습: 일별/주별/월별 OHLCV 데이터 전처리** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 일별/주별/월별 OHLCV 데이터 전처리 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 017 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 018](day018.md) 에서 계속됩니다.
