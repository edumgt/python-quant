# Day 025 — BeautifulSoup 정적 크롤링

> **모듈 5: 데이터 크롤링** | 2/5일차 | 🕷️ | 학습시간: 8시간



---

## 오늘 배울 것 (아주 쉽게)

- requests 라이브러리로 HTTP GET 요청
- BeautifulSoup 파싱 (html.parser, lxml)
- find(), find_all(), CSS 선택자 활용
- 데이터 추출 및 정제
- 실습: 뉴스 헤드라인 크롤링

---



### 1. requests 라이브러리로 HTTP GET 요청

**쉽게 이해하기**
- **requests 라이브러리로 HTTP GET 요청** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# requests 라이브러리로 HTTP GET 요청 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. BeautifulSoup 파싱 (html.parser, lxml)

**쉽게 이해하기**
- **BeautifulSoup 파싱 (html.parser, lxml)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# BeautifulSoup 파싱 (html.parser, lxml) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. find(), find_all(), CSS 선택자 활용

**쉽게 이해하기**
- **find(), find_all(), CSS 선택자 활용** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# find(), find_all(), CSS 선택자 활용 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 데이터 추출 및 정제

**쉽게 이해하기**
- **데이터 추출 및 정제** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 데이터 추출 및 정제 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 실습: 뉴스 헤드라인 크롤링

**쉽게 이해하기**
- **실습: 뉴스 헤드라인 크롤링** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 뉴스 헤드라인 크롤링 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 025 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 026](day026.md) 에서 계속됩니다.
