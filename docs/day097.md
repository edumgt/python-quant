# Day 097 — 자동화 트레이딩 시스템 구축

> **모듈 12: 나만의 투자 인디케이터 개발 및 성과 검증 프로젝트** | 13/15일차 | 📡 | 학습시간: 8시간

> 💡 **어린이 버전 안내:** 어려운 단어는 괄호 설명을 보고, 코드는 한 줄씩 천천히 따라해요.


---

## 오늘 배울 것 (아주 쉽게)

- 스케줄러(APScheduler, cron) 설정
- 인디케이터 시그널 -> 주문 자동화 파이프라인
- 예외처리 및 리스크 관리 로직
- 실습: 시그널 기반 자동 주문 시스템 프로토타입

---


## 천천히 배우기 (초등학생도 이해할 수 있어요)

### 1. 스케줄러(APScheduler, cron) 설정

**쉽게 이해하기**
- **스케줄러(APScheduler, cron) 설정**는 `작업 실행 → 로그 기록 → 상태 알림` 루프를 만들면 운영이 쉬워집니다.
- 먼저 로컬에서 한 번 자동 실행해보고, 실패 로그가 남는지 확인한 뒤 배포하세요.

**미니 예시**
```python
# 스케줄러(APScheduler, cron) 설정 미니 예시
job_name = 'rebalance-daily'
status = 'success'
log = f'[{job_name}] 상태: {status}'
print(log)
if status != 'success':
    print('알림 전송: 운영자 확인 필요')
```

### 2. 인디케이터 시그널 -> 주문 자동화 파이프라인

**쉽게 이해하기**
- **인디케이터 시그널 -> 주문 자동화 파이프라인**는 `작업 실행 → 로그 기록 → 상태 알림` 루프를 만들면 운영이 쉬워집니다.
- 먼저 로컬에서 한 번 자동 실행해보고, 실패 로그가 남는지 확인한 뒤 배포하세요.

**미니 예시**
```python
# 인디케이터 시그널 -> 주문 자동화 파이프라인 미니 예시
job_name = 'rebalance-daily'
status = 'success'
log = f'[{job_name}] 상태: {status}'
print(log)
if status != 'success':
    print('알림 전송: 운영자 확인 필요')
```

### 3. 예외처리 및 리스크 관리 로직

**쉽게 이해하기**
- **예외처리 및 리스크 관리 로직**는 `조건 점검 → 예외 대응 → 손실 제한` 순서로 설계하면 안정성이 높아집니다.
- 정상 시나리오뿐 아니라 실패 시나리오(네트워크 오류, 과도한 포지션)도 함께 테스트하세요.

**미니 예시**
```python
# 예외처리 및 리스크 관리 로직 미니 예시
max_position = 100000
order_amount = 120000
try:
    if order_amount > max_position:
        raise ValueError('리스크 한도 초과')
    print('주문 실행')
except ValueError as e:
    print('주문 차단:', e)
```

### 4. 실습: 시그널 기반 자동 주문 시스템 프로토타입

**쉽게 이해하기**
- **실습: 시그널 기반 자동 주문 시스템 프로토타입**는 `작업 실행 → 로그 기록 → 상태 알림` 루프를 만들면 운영이 쉬워집니다.
- 먼저 로컬에서 한 번 자동 실행해보고, 실패 로그가 남는지 확인한 뒤 배포하세요.

**미니 예시**
```python
# 실습: 시그널 기반 자동 주문 시스템 프로토타입 미니 예시
job_name = 'rebalance-daily'
status = 'success'
log = f'[{job_name}] 상태: {status}'
print(log)
if status != 'success':
    print('알림 전송: 운영자 확인 필요')
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 097 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 098](day098.md) 에서 계속됩니다.
