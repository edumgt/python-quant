# Day 010 — Seaborn 및 고급 시각화

> **모듈 2: Python 전처리 및 시각화** | 5/5일차 | 📊 | 학습시간: 8시간



---

## 오늘 배울 것 (아주 쉽게)

- Seaborn 개요 및 Matplotlib과의 차이
- 분포 시각화: histplot, kdeplot
- 관계 시각화: scatterplot, pairplot
- 범주형 시각화: boxplot, violinplot
- 히트맵 (heatmap) - 상관관계 행렬 시각화
- 데이터 형태별 차트 선택 가이드
- 실습: 금융 데이터 종합 시각화 대시보드

---



### 1. Seaborn 개요 및 Matplotlib과의 차이

**쉽게 이해하기**
- **Seaborn 개요 및 Matplotlib과의 차이** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# Seaborn 개요 및 Matplotlib과의 차이 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 분포 시각화: histplot, kdeplot

**쉽게 이해하기**
- **분포 시각화: histplot, kdeplot** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 분포 시각화: histplot, kdeplot 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 관계 시각화: scatterplot, pairplot

**쉽게 이해하기**
- **관계 시각화: scatterplot, pairplot** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 관계 시각화: scatterplot, pairplot 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 범주형 시각화: boxplot, violinplot

**쉽게 이해하기**
- **범주형 시각화: boxplot, violinplot** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 범주형 시각화: boxplot, violinplot 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 히트맵 (heatmap) - 상관관계 행렬 시각화

**쉽게 이해하기**
- **히트맵 (heatmap) - 상관관계 행렬 시각화** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 히트맵 (heatmap) - 상관관계 행렬 시각화 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 6. 데이터 형태별 차트 선택 가이드

**쉽게 이해하기**
- **데이터 형태별 차트 선택 가이드** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 데이터 형태별 차트 선택 가이드 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 7. 실습: 금융 데이터 종합 시각화 대시보드

**쉽게 이해하기**
- **실습: 금융 데이터 종합 시각화 대시보드** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 금융 데이터 종합 시각화 대시보드 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 010 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 011](day011.md) 에서 계속됩니다.
