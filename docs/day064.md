# Day 064 — 주가 데이터 클러스터링

> **모듈 10: 주가 지수 데이터 활용 머신러닝-딥러닝 프로젝트** | 3/10일차 | 🔬 | 학습시간: 8시간

> 💡 **어린이 버전 안내:** 어려운 단어는 괄호 설명을 보고, 코드는 한 줄씩 천천히 따라해요.


---

## 오늘 배울 것 (아주 쉽게)

- 클러스터링용 특징(Feature) 설계
- K-Means 클러스터링 적용
- 최적 클러스터 수 결정 (엘보우, 실루엣)
- 실습: 종목 군집화 수행

---


## 천천히 배우기 (초등학생도 이해할 수 있어요)

### 1. 클러스터링용 특징(Feature) 설계

**쉽게 이해하기**
- **클러스터링용 특징(Feature) 설계** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# 클러스터링용 특징(Feature) 설계 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

### 2. K-Means 클러스터링 적용

**쉽게 이해하기**
- **K-Means 클러스터링 적용** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# K-Means 클러스터링 적용 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

### 3. 최적 클러스터 수 결정 (엘보우, 실루엣)

**쉽게 이해하기**
- **최적 클러스터 수 결정 (엘보우, 실루엣)** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# 최적 클러스터 수 결정 (엘보우, 실루엣) 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

### 4. 실습: 종목 군집화 수행

**쉽게 이해하기**
- **실습: 종목 군집화 수행** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 종목 군집화 수행 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 064 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

---

## 더 공부하고 싶다면

- 강의 슬라이드: `slides/day064.pdf`
- 실습 노트북: `notebooks/day064.ipynb`

---

## 다음 시간 미리보기

➡️ [Day 065](day065.md) 에서 계속됩니다.
