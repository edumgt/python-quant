# Day 001 — 환경 설정 및 Python 기초

> **모듈 1: Python 프로그래밍** | 1/5일차 | 🐍 | 학습시간: 8시간

---

## 학습 목표

- Python 설치 및 개발 환경 구성 (Anaconda, VSCode, Jupyter Notebook)
- 변수(Variable)와 자료형 (int, float, str, bool)
- 상수(Constant) 개념 이해
- 기본 입출력 (print(), input())
- 문자열 포맷팅 (f-string, format)
- 실습: 간단한 사칙연산 계산기 구현

---

## 세부 일정

| 시간 | 내용 |
|------|------|
| 09:00 – 10:30 | 이론 강의 1 – 개념 설명 및 예제 |
| 10:30 – 10:45 | 휴식 |
| 10:45 – 12:00 | 이론 강의 2 – 심화 개념 |
| 12:00 – 13:00 | 점심 |
| 13:00 – 14:30 | 실습 1 – 코드 따라하기 |
| 14:30 – 14:45 | 휴식 |
| 14:45 – 16:30 | 실습 2 – 응용 문제 풀기 |
| 16:30 – 17:00 | 복습 및 Q&A |

---

## 학습 내용

### 1. Python 설치 및 개발 환경 구성 (Anaconda, VSCode, Jupyter Notebook)

**쉽게 이해하기**
- **Python 설치 및 개발 환경 구성 (Anaconda, VSCode, Jupyter Notebook)** 개념은 작은 변수와 함수부터 직접 실행해보면 가장 빨리 익숙해집니다.
- 한 번에 길게 작성하기보다, 한 줄씩 출력 결과를 확인하면서 진행하세요.

**미니 예시**
```python
# Python 설치 및 개발 환경 구성 (Anaconda, VSCode, Jupyter Notebook) 미니 예시
name = 'quant'
scores = [80, 90, 100]
avg = sum(scores) / len(scores)
print(f'{name} 평균 점수: {avg:.1f}')
```

### 2. 변수(Variable)와 자료형 (int, float, str, bool)

**쉽게 이해하기**
- **변수(Variable)와 자료형 (int, float, str, bool)** 개념은 작은 변수와 함수부터 직접 실행해보면 가장 빨리 익숙해집니다.
- 한 번에 길게 작성하기보다, 한 줄씩 출력 결과를 확인하면서 진행하세요.

**미니 예시**
```python
# 변수(Variable)와 자료형 (int, float, str, bool) 미니 예시
name = 'quant'
scores = [80, 90, 100]
avg = sum(scores) / len(scores)
print(f'{name} 평균 점수: {avg:.1f}')
```

### 3. 상수(Constant) 개념 이해

**쉽게 이해하기**
- **상수(Constant) 개념 이해** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 상수(Constant) 개념 이해 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 기본 입출력 (print(), input())

**쉽게 이해하기**
- **기본 입출력 (print(), input())** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 기본 입출력 (print(), input()) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 문자열 포맷팅 (f-string, format)

**쉽게 이해하기**
- **문자열 포맷팅 (f-string, format)** 개념은 작은 변수와 함수부터 직접 실행해보면 가장 빨리 익숙해집니다.
- 한 번에 길게 작성하기보다, 한 줄씩 출력 결과를 확인하면서 진행하세요.

**미니 예시**
```python
# 문자열 포맷팅 (f-string, format) 미니 예시
name = 'quant'
scores = [80, 90, 100]
avg = sum(scores) / len(scores)
print(f'{name} 평균 점수: {avg:.1f}')
```

### 6. 실습: 간단한 사칙연산 계산기 구현

**쉽게 이해하기**
- **실습: 간단한 사칙연산 계산기 구현** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 간단한 사칙연산 계산기 구현 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 실습 과제

오늘 학습한 내용을 바탕으로 아래 과제를 완성하세요.

```python
# Day 001 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

---

## 참고 자료

- 강의 슬라이드: `slides/day001.pdf`
- 실습 노트북: `notebooks/day001.ipynb`

---

## 다음 학습 예고

➡️ [Day 002](day002.md) 에서 계속됩니다.
