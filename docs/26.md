# Day 040 — 모델 성능 평가 및 개선

> **모듈 6: 퀀트를 위한 머신러닝과 딥러닝** | 12/13일차 | 🤖 | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 머신러닝 모델 성능 평가 개선](https://www.youtube.com/results?search_query=머신러닝+모델+성능평가+하이퍼파라미터+파이썬+한국어)

## 오늘 배울 것 (아주 쉽게)

- 회귀 지표: MAE, RMSE, MAPE
- 분류 지표: Confusion Matrix, ROC-AUC
- 과적합 진단 및 정규화 (L1, L2)
- 학습률 스케줄러 및 조기 종료(Early Stopping)
- 실습: 모델 성능 비교 및 최적 모델 선택

---



### 1. 회귀 지표: MAE, RMSE, MAPE

**쉽게 이해하기**
- **회귀 지표: MAE, RMSE, MAPE** 개념은 `입력(x)와 출력(y)의 관계 찾기`라고 생각하면 쉽습니다.
- 예측값만 보지 말고, 실제값과의 차이(오차)를 같이 확인하세요.

**미니 예시**
```python
# 회귀 지표: MAE, RMSE, MAPE 미니 예시
x = [1, 2, 3, 4]
y = [2, 4, 6, 8]
slope = (y[-1] - y[0]) / (x[-1] - x[0])
next_y = y[-1] + slope
print('다음 값 예측:', next_y)
```

### 2. 분류 지표: Confusion Matrix, ROC-AUC

**쉽게 이해하기**
- **분류 지표: Confusion Matrix, ROC-AUC** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# 분류 지표: Confusion Matrix, ROC-AUC 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

### 3. 과적합 진단 및 정규화 (L1, L2)

**쉽게 이해하기**
- **과적합 진단 및 정규화 (L1, L2)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 과적합 진단 및 정규화 (L1, L2) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 학습률 스케줄러 및 조기 종료(Early Stopping)

**쉽게 이해하기**
- **학습률 스케줄러 및 조기 종료(Early Stopping)**는 `작업 실행 → 로그 기록 → 상태 알림` 루프를 만들면 운영이 쉬워집니다.
- 먼저 로컬에서 한 번 자동 실행해보고, 실패 로그가 남는지 확인한 뒤 배포하세요.

**미니 예시**
```python
# 학습률 스케줄러 및 조기 종료(Early Stopping) 미니 예시
job_name = 'rebalance-daily'
status = 'success'
log = f'[{job_name}] 상태: {status}'
print(log)
if status != 'success':
    print('알림 전송: 운영자 확인 필요')
```

### 5. 실습: 모델 성능 비교 및 최적 모델 선택

**쉽게 이해하기**
- **실습: 모델 성능 비교 및 최적 모델 선택** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 모델 성능 비교 및 최적 모델 선택 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 040 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 041](26.md) 에서 계속됩니다.



---


# Day 041 — ML/DL 모듈 종합 실습 (반일)

> **모듈 6: 퀀트를 위한 머신러닝과 딥러닝** | 13/13일차 | 🤖 | 학습시간: 4시간 (반일)



---

> 📺 **YouTube 강의**: [🎬 ML DL 종합 실습 파이썬](https://www.youtube.com/results?search_query=머신러닝+딥러닝+종합+실습+파이썬+한국어)

## 오늘 배울 것 (아주 쉽게)

- 모듈 전체 복습 및 Q&A
- 주요 모델 비교표 정리
- 종합 실습: 다양한 모델 앙상블로 주가 예측 성능 향상

---



### 1. 모듈 전체 복습 및 Q&A

**쉽게 이해하기**
- **모듈 전체 복습 및 Q&A**는 새 이론을 늘리기보다, 지금까지 만든 결과를 구조적으로 정리하는 단계입니다.
- 핵심 개념, 구현 근거, 한계와 개선안까지 1장표로 요약하면 전달력이 높아집니다.

**미니 예시**
```python
# 모듈 전체 복습 및 Q&A 미니 예시
checklist = {
    '핵심개념정리': True,
    '전략시연': True,
    '성과지표설명': False
}
done = sum(checklist.values())
print(f'발표 준비 완료율: {done / len(checklist):.0%}')
```

### 2. 주요 모델 비교표 정리

**쉽게 이해하기**
- **주요 모델 비교표 정리** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 주요 모델 비교표 정리 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 종합 실습: 다양한 모델 앙상블로 주가 예측 성능 향상

**쉽게 이해하기**
- **종합 실습: 다양한 모델 앙상블로 주가 예측 성능 향상** 개념은 `입력(x)와 출력(y)의 관계 찾기`라고 생각하면 쉽습니다.
- 예측값만 보지 말고, 실제값과의 차이(오차)를 같이 확인하세요.

**미니 예시**
```python
# 종합 실습: 다양한 모델 앙상블로 주가 예측 성능 향상 미니 예시
x = [1, 2, 3, 4]
y = [2, 4, 6, 8]
slope = (y[-1] - y[0]) / (x[-1] - x[0])
next_y = y[-1] + slope
print('다음 값 예측:', next_y)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 041 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 042](27.md) 에서 계속됩니다.


---

<!-- merged from Chapter14.md -->

# Chapter 14. NLP Text Classification — 텍스트 분류

> 💡 **쉽게 이해하기**: 텍스트를 숫자로 변환(TF-IDF)한 후 어떤 주제인지 분류하는 NLP의 기본 파이프라인입니다. "이 기사는 스포츠 기사인가, 정치 기사인가?"처럼 텍스트의 주제를 자동으로 분류할 수 있습니다.

---

### 핵심 개념

자연어 처리(NLP)의 첫 단계는 텍스트를 숫자 벡터로 변환하는 것입니다.  
이 실습에서는 **TF-IDF 벡터화 → 로지스틱 회귀** 파이프라인으로 텍스트를 분류합니다.

### TF-IDF란?

- **TF(Term Frequency)**: 한 문서 내 단어 빈도
- **IDF(Inverse Document Frequency)**: 전체 문서에서 희귀할수록 높은 가중치
- 흔한 단어(the, is)는 낮은 점수, 특징적인 단어는 높은 점수

### 파이프라인

```python
pipeline = Pipeline([
    ('tfidf', TfidfVectorizer(max_features=5000, ngram_range=(1, 2))),
    ('clf',   LogisticRegression()),
])
```

1. `TfidfVectorizer`: 텍스트 → 희소 행렬 (문서 × 단어 수)
2. `ngram_range=(1,2)`: 단어 + 2-gram 패턴 포함
3. `LogisticRegression`: 희소 벡터 분류

### 데이터셋

20 Newsgroups 중 2개 카테고리 사용:
- `rec.sport.hockey` → 스포츠
- `talk.politics.misc` → 정치

### API 확장 포인트

- 사용자가 직접 입력한 텍스트를 실시간 분류
- 신뢰도(confidence)와 함께 결과 표시
- 더 많은 카테고리로 확장 가능

---

## 📺 참고 유튜브 영상

| 기술 스택 | 채널 | 링크 |
|---------|------|------|
| TF-IDF 개념 | StatQuest | [TF-IDF (Term Frequency - Inverse Document Frequency)](https://www.youtube.com/watch?v=OkFdqqyI8y4) |
| scikit-learn Pipeline | Data School | [Building a Machine Learning Pipeline](https://www.youtube.com/watch?v=irHhDMbw3xo) |
| BERT 입문 | HuggingFace | [Fine-tuning BERT for Text Classification](https://www.youtube.com/watch?v=GSt00_-0ncQ) |
| NLP 기초 개념 | StatQuest | [Word Embedding and Word2Vec, Clearly Explained](https://www.youtube.com/watch?v=viZrOnJclY0) |
