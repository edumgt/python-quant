# Day 072 — 로보 어드바이저 개요 및 프로젝트 설계

> **모듈 11: 나만의 로보 어드바이저 개발 및 성과 검증 프로젝트** | 1/13일차 | 🤖 | 학습시간: 8시간

> 💡 **어린이 버전 안내:** 어려운 단어는 괄호 설명을 보고, 코드는 한 줄씩 천천히 따라해요.


---

## 오늘 배울 것 (아주 쉽게)

- 로보 어드바이저 정의 및 구성요소
- 국내외 로보 어드바이저 사례 분석
- 프로젝트 아키텍처 설계
- 데이터 파이프라인 계획
- 실습: 프로젝트 요구사항 정의서 작성

---


## 천천히 배우기 (초등학생도 이해할 수 있어요)

### 1. 로보 어드바이저 정의 및 구성요소

**쉽게 이해하기**
- **로보 어드바이저 정의 및 구성요소** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 로보 어드바이저 정의 및 구성요소 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 국내외 로보 어드바이저 사례 분석

**쉽게 이해하기**
- **국내외 로보 어드바이저 사례 분석** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 국내외 로보 어드바이저 사례 분석 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 프로젝트 아키텍처 설계

**쉽게 이해하기**
- **프로젝트 아키텍처 설계**는 새 이론을 늘리기보다, 지금까지 만든 결과를 구조적으로 정리하는 단계입니다.
- 핵심 개념, 구현 근거, 한계와 개선안까지 1장표로 요약하면 전달력이 높아집니다.

**미니 예시**
```python
# 프로젝트 아키텍처 설계 미니 예시
checklist = {
    '핵심개념정리': True,
    '전략시연': True,
    '성과지표설명': False
}
done = sum(checklist.values())
print(f'발표 준비 완료율: {done / len(checklist):.0%}')
```

### 4. 데이터 파이프라인 계획

**쉽게 이해하기**
- **데이터 파이프라인 계획**는 `작업 실행 → 로그 기록 → 상태 알림` 루프를 만들면 운영이 쉬워집니다.
- 먼저 로컬에서 한 번 자동 실행해보고, 실패 로그가 남는지 확인한 뒤 배포하세요.

**미니 예시**
```python
# 데이터 파이프라인 계획 미니 예시
job_name = 'rebalance-daily'
status = 'success'
log = f'[{job_name}] 상태: {status}'
print(log)
if status != 'success':
    print('알림 전송: 운영자 확인 필요')
```

### 5. 실습: 프로젝트 요구사항 정의서 작성

**쉽게 이해하기**
- **실습: 프로젝트 요구사항 정의서 작성**는 새 이론을 늘리기보다, 지금까지 만든 결과를 구조적으로 정리하는 단계입니다.
- 핵심 개념, 구현 근거, 한계와 개선안까지 1장표로 요약하면 전달력이 높아집니다.

**미니 예시**
```python
# 실습: 프로젝트 요구사항 정의서 작성 미니 예시
checklist = {
    '핵심개념정리': True,
    '전략시연': True,
    '성과지표설명': False
}
done = sum(checklist.values())
print(f'발표 준비 완료율: {done / len(checklist):.0%}')
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 072 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 073](day073.md) 에서 계속됩니다.
