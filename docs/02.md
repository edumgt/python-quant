# Day 007 — Pandas 기초

> **모듈 2: Python 전처리 및 시각화** | 2/5일차 | 📊 | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 Pandas 기초 데이터분석](https://www.youtube.com/results?search_query=Pandas+기초+데이터프레임+파이썬+한국어+강의)

## 오늘 배울 것 (아주 쉽게)

- Series와 DataFrame 개념 및 생성
- CSV / Excel 파일 읽기·쓰기 (read_csv, to_csv)
- 데이터 조회: head(), tail(), info(), describe()
- 인덱싱: loc, iloc
- 컬럼 추가 및 삭제
- 실습: 주가 데이터 DataFrame 구성 및 탐색

---



### 1. Series와 DataFrame 개념 및 생성

**쉽게 이해하기**
- **Series와 DataFrame 개념 및 생성** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# Series와 DataFrame 개념 및 생성 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. CSV / Excel 파일 읽기·쓰기 (read_csv, to_csv)

**쉽게 이해하기**
- **CSV / Excel 파일 읽기·쓰기 (read_csv, to_csv)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# CSV / Excel 파일 읽기·쓰기 (read_csv, to_csv) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 데이터 조회: head(), tail(), info(), describe()

**쉽게 이해하기**
- **데이터 조회: head(), tail(), info(), describe()** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 데이터 조회: head(), tail(), info(), describe() 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 인덱싱: loc, iloc

**쉽게 이해하기**
- **인덱싱: loc, iloc** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 인덱싱: loc, iloc 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 컬럼 추가 및 삭제

**쉽게 이해하기**
- **컬럼 추가 및 삭제** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 컬럼 추가 및 삭제 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 6. 실습: 주가 데이터 DataFrame 구성 및 탐색

**쉽게 이해하기**
- **실습: 주가 데이터 DataFrame 구성 및 탐색** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 주가 데이터 DataFrame 구성 및 탐색 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 007 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 008](02.md) 에서 계속됩니다.



---


# Day 008 — Pandas 데이터 전처리

> **모듈 2: Python 전처리 및 시각화** | 3/5일차 | 📊 | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 Pandas 데이터 전처리](https://www.youtube.com/results?search_query=Pandas+데이터+전처리+결측치+파이썬+한국어)

## 오늘 배울 것 (아주 쉽게)

- 결측치(Missing Value) 탐지 및 처리 (dropna, fillna)
- 중복 데이터 처리 (drop_duplicates)
- 데이터 필터링 및 조건 선택
- 그룹화 (groupby) 및 집계
- 데이터 합치기 (merge, concat, join)
- 날짜/시간 데이터 처리 (pd.to_datetime)
- 실습: 금융 데이터 정제 파이프라인 구성

---



### 1. 결측치(Missing Value) 탐지 및 처리 (dropna, fillna)

**쉽게 이해하기**
- **결측치(Missing Value) 탐지 및 처리 (dropna, fillna)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 결측치(Missing Value) 탐지 및 처리 (dropna, fillna) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 중복 데이터 처리 (drop_duplicates)

**쉽게 이해하기**
- **중복 데이터 처리 (drop_duplicates)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 중복 데이터 처리 (drop_duplicates) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 데이터 필터링 및 조건 선택

**쉽게 이해하기**
- **데이터 필터링 및 조건 선택** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 데이터 필터링 및 조건 선택 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 그룹화 (groupby) 및 집계

**쉽게 이해하기**
- **그룹화 (groupby) 및 집계** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 그룹화 (groupby) 및 집계 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 데이터 합치기 (merge, concat, join)

**쉽게 이해하기**
- **데이터 합치기 (merge, concat, join)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 데이터 합치기 (merge, concat, join) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 6. 날짜/시간 데이터 처리 (pd.to_datetime)

**쉽게 이해하기**
- **날짜/시간 데이터 처리 (pd.to_datetime)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 날짜/시간 데이터 처리 (pd.to_datetime) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 7. 실습: 금융 데이터 정제 파이프라인 구성

**쉽게 이해하기**
- **실습: 금융 데이터 정제 파이프라인 구성**는 `작업 실행 → 로그 기록 → 상태 알림` 루프를 만들면 운영이 쉬워집니다.
- 먼저 로컬에서 한 번 자동 실행해보고, 실패 로그가 남는지 확인한 뒤 배포하세요.

**미니 예시**
```python
# 실습: 금융 데이터 정제 파이프라인 구성 미니 예시
job_name = 'rebalance-daily'
status = 'success'
log = f'[{job_name}] 상태: {status}'
print(log)
if status != 'success':
    print('알림 전송: 운영자 확인 필요')
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 008 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 009](03.md) 에서 계속됩니다.
