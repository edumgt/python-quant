# Day 011 — 기초 통계량

> **모듈 3: AI 및 퀀트 모델링을 위한 기초 수학-통계** | 1/5일차 | 📐 | 학습시간: 8시간



---

## 오늘 배울 것 (아주 쉽게)

- 모집단과 표본 개념
- 평균(Mean), 중위값(Median), 최빈값(Mode)
- 분산(Variance)과 표준편차(Standard Deviation)
- 사분위수(Quartile)와 IQR
- 첨도(Kurtosis)와 왜도(Skewness)
- 실습: NumPy / SciPy로 주가 수익률 통계량 계산

---



### 1. 모집단과 표본 개념

**쉽게 이해하기**
- **모집단과 표본 개념** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 모집단과 표본 개념 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 평균(Mean), 중위값(Median), 최빈값(Mode)

**쉽게 이해하기**
- **평균(Mean), 중위값(Median), 최빈값(Mode)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 평균(Mean), 중위값(Median), 최빈값(Mode) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 분산(Variance)과 표준편차(Standard Deviation)

**쉽게 이해하기**
- **분산(Variance)과 표준편차(Standard Deviation)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 분산(Variance)과 표준편차(Standard Deviation) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 사분위수(Quartile)와 IQR

**쉽게 이해하기**
- **사분위수(Quartile)와 IQR** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 사분위수(Quartile)와 IQR 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 첨도(Kurtosis)와 왜도(Skewness)

**쉽게 이해하기**
- **첨도(Kurtosis)와 왜도(Skewness)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 첨도(Kurtosis)와 왜도(Skewness) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 6. 실습: NumPy / SciPy로 주가 수익률 통계량 계산

**쉽게 이해하기**
- **실습: NumPy / SciPy로 주가 수익률 통계량 계산** 개념은 `일별 수익률 계산 → 누적 성과 확인 → 리스크 지표 비교`로 이해하면 쉽습니다.
- 먼저 작은 구간에서 계산이 맞는지 검증한 뒤, 전체 기간으로 확장하세요.

**미니 예시**
```python
# 실습: NumPy / SciPy로 주가 수익률 통계량 계산 미니 예시
returns = [0.01, -0.02, 0.015, 0.005]
cum = 1.0
for r in returns:
    cum *= (1 + r)
print(f'누적수익률: {(cum - 1):.2%}')
```

---

## 오늘 내용 한눈에 정리

- **평균·중위값·최빈값**은 데이터의 대표값을 보는 도구입니다.
- **분산·표준편차**는 값들이 평균에서 얼마나 멀리 퍼져 있는지 보여줍니다.
- **사분위수·IQR**은 데이터의 가운데 구간이 어디인지 알려줍니다.
- **왜도·첨도**는 분포 모양(치우침, 뾰족함)을 설명해 줍니다.

## 용어 사전 (초등학생도 이해하기 쉽게)

| 용어 | 한자 | 영어 | 쉬운 설명 |
|---|---|---|---|
| 통계 | 統計 | Statistics | 많은 숫자를 모아서 규칙을 찾는 방법 |
| 모집단 | 母集團 | Population | 조사하고 싶은 전체 그룹 |
| 표본 | 標本 | Sample | 전체에서 뽑은 작은 일부 |
| 평균 | 平均 | Mean | 값을 다 더해 똑같이 나눈 값 |
| 분산 | 分散 | Variance | 값들이 평균에서 얼마나 퍼졌는지 |
| 표준편차 | 標準偏差 | Standard Deviation | 퍼진 정도를 보기 쉬운 숫자로 바꾼 것 |

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 011 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 012](day012.md) 에서 계속됩니다.
