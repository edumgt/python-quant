# Day 004 — 객체지향 프로그래밍 기초 (OOP)

> **모듈 1: Python 프로그래밍** | 4/5일차 | 🐍 | 학습시간: 8시간

---

## 학습 목표

- 객체지향 개념 이해 (클래스 vs 인스턴스)
- 클래스 정의 (class)와 인스턴스 생성
- 속성(Attribute)과 메서드(Method)
- 생성자 (__init__)
- 캡슐화(Encapsulation): private, public
- 실습: 주식 종목 클래스 설계

---

## 세부 일정

| 시간 | 내용 |
|------|------|
| 09:00 – 10:30 | 이론 강의 1 – 개념 설명 및 예제 |
| 10:30 – 10:45 | 휴식 |
| 10:45 – 12:00 | 이론 강의 2 – 심화 개념 |
| 12:00 – 13:00 | 점심 |
| 13:00 – 14:30 | 실습 1 – 코드 따라하기 |
| 14:30 – 14:45 | 휴식 |
| 14:45 – 16:30 | 실습 2 – 응용 문제 풀기 |
| 16:30 – 17:00 | 복습 및 Q&A |

---

## 학습 내용

### 1. 객체지향 개념 이해 (클래스 vs 인스턴스)

**쉽게 이해하기**
- **객체지향 개념 이해 (클래스 vs 인스턴스)** 개념은 작은 변수와 함수부터 직접 실행해보면 가장 빨리 익숙해집니다.
- 한 번에 길게 작성하기보다, 한 줄씩 출력 결과를 확인하면서 진행하세요.

**미니 예시**
```python
# 객체지향 개념 이해 (클래스 vs 인스턴스) 미니 예시
name = 'quant'
scores = [80, 90, 100]
avg = sum(scores) / len(scores)
print(f'{name} 평균 점수: {avg:.1f}')
```

### 2. 클래스 정의 (class)와 인스턴스 생성

**쉽게 이해하기**
- **클래스 정의 (class)와 인스턴스 생성** 개념은 작은 변수와 함수부터 직접 실행해보면 가장 빨리 익숙해집니다.
- 한 번에 길게 작성하기보다, 한 줄씩 출력 결과를 확인하면서 진행하세요.

**미니 예시**
```python
# 클래스 정의 (class)와 인스턴스 생성 미니 예시
name = 'quant'
scores = [80, 90, 100]
avg = sum(scores) / len(scores)
print(f'{name} 평균 점수: {avg:.1f}')
```

### 3. 속성(Attribute)과 메서드(Method)

**쉽게 이해하기**
- **속성(Attribute)과 메서드(Method)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 속성(Attribute)과 메서드(Method) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 생성자 (__init__)

**쉽게 이해하기**
- **생성자 (__init__)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 생성자 (__init__) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 캡슐화(Encapsulation): private, public

**쉽게 이해하기**
- **캡슐화(Encapsulation): private, public** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 캡슐화(Encapsulation): private, public 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 6. 실습: 주식 종목 클래스 설계

**쉽게 이해하기**
- **실습: 주식 종목 클래스 설계** 개념은 작은 변수와 함수부터 직접 실행해보면 가장 빨리 익숙해집니다.
- 한 번에 길게 작성하기보다, 한 줄씩 출력 결과를 확인하면서 진행하세요.

**미니 예시**
```python
# 실습: 주식 종목 클래스 설계 미니 예시
name = 'quant'
scores = [80, 90, 100]
avg = sum(scores) / len(scores)
print(f'{name} 평균 점수: {avg:.1f}')
```

---

## 실습 과제

오늘 학습한 내용을 바탕으로 아래 과제를 완성하세요.

```python
# Day 004 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

---

## 참고 자료

- 강의 슬라이드: `slides/day004.pdf`
- 실습 노트북: `notebooks/day004.ipynb`

---

## 다음 학습 예고

➡️ [Day 005](day005.md) 에서 계속됩니다.
