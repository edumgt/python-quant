# Day 031 — SVM (Support Vector Machine)

> **모듈 6: 퀀트를 위한 머신러닝과 딥러닝** | 3/13일차 | 🤖 | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 SVM 서포트벡터머신 파이썬](https://www.youtube.com/results?search_query=SVM+서포트벡터머신+파이썬+한국어+강의)

## 오늘 배울 것 (아주 쉽게)

- SVM 이론 (최대 마진 분류기)
- 커널 트릭 (RBF, Polynomial)
- SVR (Support Vector Regression)
- 하이퍼파라미터: C, gamma
- 실습: SVM으로 주가 방향성 분류

---



### 1. SVM 이론 (최대 마진 분류기)

**쉽게 이해하기**
- **SVM 이론 (최대 마진 분류기)** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# SVM 이론 (최대 마진 분류기) 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

### 2. 커널 트릭 (RBF, Polynomial)

**쉽게 이해하기**
- **커널 트릭 (RBF, Polynomial)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 커널 트릭 (RBF, Polynomial) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. SVR (Support Vector Regression)

**쉽게 이해하기**
- **SVR (Support Vector Regression)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# SVR (Support Vector Regression) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 하이퍼파라미터: C, gamma

**쉽게 이해하기**
- **하이퍼파라미터: C, gamma** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 하이퍼파라미터: C, gamma 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 실습: SVM으로 주가 방향성 분류

**쉽게 이해하기**
- **실습: SVM으로 주가 방향성 분류** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# 실습: SVM으로 주가 방향성 분류 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 031 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 032](18.md) 에서 계속됩니다.


---

<!-- merged from Chapter03.md -->

# Chapter 03. Decision Boundary 시각화 해설

> 💡 **쉽게 이해하기**: 빨간 공과 파란 공이 섞인 지도에서 두 그룹을 나누는 경계선을 그리는 것입니다. 모델이 공간을 어떻게 분류하는지 색깔 지도로 눈으로 직접 확인할 수 있어 머신러닝 개념 이해에 큰 도움이 됩니다.

---

`DecisionBoundary.py`는 모델이 공간을 어떻게 나누는지 시각적으로 보여줍니다.

### 핵심 개념
- 입력 특징이 2차원일 때, 모델은 평면 위의 경계를 학습
- 경계 한쪽은 클래스 0, 다른 쪽은 클래스 1로 예측

### 구현 흐름
1. 2차원 데이터 생성
2. 학습/검증 분리
3. 로지스틱 회귀 학습
4. `meshgrid`로 촘촘한 점 생성
5. 각 점 예측 결과를 색으로 렌더링

### API/클라우드 관점
- 서버에서 PNG를 생성해 base64로 전달
- 브라우저는 `<img>`로 즉시 표시
- 원격 환경에서도 `plt.show()` 없이 동일한 시각화 제공

---

## 📺 참고 유튜브 영상

| 기술 스택 | 채널 | 링크 |
|---------|------|------|
| Logistic Regression | StatQuest | [Logistic Regression, Clearly Explained](https://www.youtube.com/watch?v=yIYKR4sgzI8) |
| Decision Boundary 개념 | StatQuest | [Decision Trees, Part 1](https://www.youtube.com/watch?v=7VeUPuFGJHk) |
| matplotlib 시각화 | Corey Schafer | [Matplotlib Tutorial Series](https://www.youtube.com/watch?v=UO98lJQ3QGI) |


---

<!-- merged from Chapter12.md -->

# Chapter 12. SVM Classifier — 서포트 벡터 머신

> 💡 **쉽게 이해하기**: 두 그룹 사이에 경계선을 긋되, 양쪽 그룹과 가장 멀리 떨어진 선을 찾는 분류기입니다. 커널을 바꾸면 직선뿐만 아니라 곡선 경계도 학습할 수 있어, 복잡한 데이터 패턴에도 강력하게 사용됩니다.

---

### 핵심 개념

SVM은 클래스 경계에서 **마진(margin)**을 최대화하는 분류기입니다.  
서포트 벡터(경계에 가장 가까운 샘플)가 결정 경계를 정의합니다.

### 커널 함수

| 커널 | 특징 | 사용 상황 |
|------|------|---------|
| `linear` | 직선 경계 | 선형 분리 가능 데이터 |
| `rbf` | 가우시안 비선형 경계 | 일반적인 분류 문제 (기본값) |
| `poly` | 다항식 경계 | 곡선 패턴 |

### C 파라미터 (정규화)

- **작은 C** → 넓은 마진, 오분류 허용 (underfitting 위험)
- **큰 C** → 좁은 마진, 오분류 최소화 (overfitting 위험)

### 구현 흐름

1. 2D 분류 데이터 생성 (`make_classification`)
2. `StandardScaler` 정규화
3. `SVC(kernel=..., C=...)` 학습
4. `DecisionBoundaryDisplay`로 시각화
5. 서포트 벡터를 원으로 강조 표시

### API 확장 시 장점

- 커널과 C 값을 실시간으로 바꿔가며 결정 경계 변화 비교
- 서포트 벡터 수와 정확도를 함께 확인

---

## 📺 참고 유튜브 영상

| 기술 스택 | 채널 | 링크 |
|---------|------|------|
| SVM 핵심 개념 | StatQuest | [Support Vector Machines, Clearly Explained!!!](https://www.youtube.com/watch?v=efR1C6CvhmE) |
| Kernel Trick (RBF) | StatQuest | [Support Vector Machines - The Polynomial Kernel](https://www.youtube.com/watch?v=Toet3EiSFcM) |
| C 파라미터 / 마진 | StatQuest | [Support Vector Machines - The Soft Margin](https://www.youtube.com/watch?v=IpKLTFGVMzs) |
