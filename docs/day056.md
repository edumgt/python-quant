# Day 056 — 자산배분 모델

> **모듈 8: 퀀트를 위한 금융 필수 지식** | 5/5일차 | 🏦 | 학습시간: 8시간

> 💡 **어린이 버전 안내:** 어려운 단어는 괄호 설명을 보고, 코드는 한 줄씩 천천히 따라해요.


---

## 오늘 배울 것 (아주 쉽게)

- 평균분산 최적화(Mean-Variance Optimization)
- 블랙-리터만(Black-Litterman) 모델 개요
- Risk-Parity 모델 개요
- 자산배분 사례 분석 (60/40, All Weather 등)
- 실습: Python으로 평균분산 자산배분 구현

---


## 천천히 배우기 (초등학생도 이해할 수 있어요)

### 1. 평균분산 최적화(Mean-Variance Optimization)

**쉽게 이해하기**
- **평균분산 최적화(Mean-Variance Optimization)** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# 평균분산 최적화(Mean-Variance Optimization) 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 2. 블랙-리터만(Black-Litterman) 모델 개요

**쉽게 이해하기**
- **블랙-리터만(Black-Litterman) 모델 개요** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# 블랙-리터만(Black-Litterman) 모델 개요 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 3. Risk-Parity 모델 개요

**쉽게 이해하기**
- **Risk-Parity 모델 개요** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# Risk-Parity 모델 개요 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 4. 자산배분 사례 분석 (60/40, All Weather 등)

**쉽게 이해하기**
- **자산배분 사례 분석 (60/40, All Weather 등)** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# 자산배분 사례 분석 (60/40, All Weather 등) 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 5. 실습: Python으로 평균분산 자산배분 구현

**쉽게 이해하기**
- **실습: Python으로 평균분산 자산배분 구현** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# 실습: Python으로 평균분산 자산배분 구현 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 056 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

---

## 더 공부하고 싶다면

- 강의 슬라이드: `slides/day056.pdf`
- 실습 노트북: `notebooks/day056.ipynb`

---

## 다음 시간 미리보기

➡️ [Day 057](day057.md) 에서 계속됩니다.
