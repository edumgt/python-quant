# Day 027 — Selenium 동적 크롤링

> **모듈 5: 데이터 크롤링** | 4/5일차 | 🕷️ | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 Selenium 동적 크롤링 파이썬](https://www.youtube.com/results?search_query=Selenium+동적+크롤링+파이썬+한국어+강의)

## 오늘 배울 것 (아주 쉽게)

- Selenium 설치 및 WebDriver 설정
- 요소 탐색 (find_element, By 선택자)
- 클릭, 입력, 스크롤 자동화
- JavaScript 실행 (execute_script)
- 실습: 동적 로딩 금융 데이터 수집

---



### 1. Selenium 설치 및 WebDriver 설정

**쉽게 이해하기**
- **Selenium 설치 및 WebDriver 설정** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# Selenium 설치 및 WebDriver 설정 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 요소 탐색 (find_element, By 선택자)

**쉽게 이해하기**
- **요소 탐색 (find_element, By 선택자)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 요소 탐색 (find_element, By 선택자) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 클릭, 입력, 스크롤 자동화

**쉽게 이해하기**
- **클릭, 입력, 스크롤 자동화** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 클릭, 입력, 스크롤 자동화 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. JavaScript 실행 (execute_script)

**쉽게 이해하기**
- **JavaScript 실행 (execute_script)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# JavaScript 실행 (execute_script) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 실습: 동적 로딩 금융 데이터 수집

**쉽게 이해하기**
- **실습: 동적 로딩 금융 데이터 수집** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 동적 로딩 금융 데이터 수집 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 027 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 028](15.md) 에서 계속됩니다.
