# Day 008 — Pandas 데이터 전처리

> **모듈 2: Python 전처리 및 시각화** | 3/5일차 | 📊 | 학습시간: 8시간

---

## 학습 목표

- 결측치(Missing Value) 탐지 및 처리 (dropna, fillna)
- 중복 데이터 처리 (drop_duplicates)
- 데이터 필터링 및 조건 선택
- 그룹화 (groupby) 및 집계
- 데이터 합치기 (merge, concat, join)
- 날짜/시간 데이터 처리 (pd.to_datetime)
- 실습: 금융 데이터 정제 파이프라인 구성

---

## 세부 일정

| 시간 | 내용 |
|------|------|
| 09:00 – 10:30 | 이론 강의 1 – 개념 설명 및 예제 |
| 10:30 – 10:45 | 휴식 |
| 10:45 – 12:00 | 이론 강의 2 – 심화 개념 |
| 12:00 – 13:00 | 점심 |
| 13:00 – 14:30 | 실습 1 – 코드 따라하기 |
| 14:30 – 14:45 | 휴식 |
| 14:45 – 16:30 | 실습 2 – 응용 문제 풀기 |
| 16:30 – 17:00 | 복습 및 Q&A |

---

## 학습 내용

### 1. 결측치(Missing Value) 탐지 및 처리 (dropna, fillna)

**쉽게 이해하기**
- **결측치(Missing Value) 탐지 및 처리 (dropna, fillna)** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 결측치(Missing Value) 탐지 및 처리 (dropna, fillna) 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

### 2. 중복 데이터 처리 (drop_duplicates)

**쉽게 이해하기**
- **중복 데이터 처리 (drop_duplicates)** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 중복 데이터 처리 (drop_duplicates) 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

### 3. 데이터 필터링 및 조건 선택

**쉽게 이해하기**
- **데이터 필터링 및 조건 선택** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 데이터 필터링 및 조건 선택 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

### 4. 그룹화 (groupby) 및 집계

**쉽게 이해하기**
- **그룹화 (groupby) 및 집계** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 그룹화 (groupby) 및 집계 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

### 5. 데이터 합치기 (merge, concat, join)

**쉽게 이해하기**
- **데이터 합치기 (merge, concat, join)** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 데이터 합치기 (merge, concat, join) 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

### 6. 날짜/시간 데이터 처리 (pd.to_datetime)

**쉽게 이해하기**
- **날짜/시간 데이터 처리 (pd.to_datetime)** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 날짜/시간 데이터 처리 (pd.to_datetime) 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

### 7. 실습: 금융 데이터 정제 파이프라인 구성

**쉽게 이해하기**
- **실습: 금융 데이터 정제 파이프라인 구성** 개념은 복잡하게 외우기보다, `입력 데이터 → 규칙 적용 → 결과 확인` 흐름으로 보면 쉽습니다.
- 먼저 작은 샘플로 동작을 확인한 뒤, 실제 데이터로 확장하면 실수가 줄어듭니다.

**미니 예시**
```python
# 실습: 금융 데이터 정제 파이프라인 구성 미니 예시
sample = [100, 102, 101, 105]
change_rate = (sample[-1] - sample[0]) / sample[0]
print(f"변화율: {change_rate:.2%}")
```

---

## 실습 과제

오늘 학습한 내용을 바탕으로 아래 과제를 완성하세요.

```python
# Day 008 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

---

## 참고 자료

- 강의 슬라이드: `slides/day008.pdf`
- 실습 노트북: `notebooks/day008.ipynb`

---

## 다음 학습 예고

➡️ [Day 009](day009.md) 에서 계속됩니다.
