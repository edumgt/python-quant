# Day 024 — HTML 및 웹 구조 이해

> **모듈 5: 데이터 크롤링** | 1/5일차 | 🕷️ | 학습시간: 8시간

---

## 학습 목표

- HTML 태그 기초 (div, span, table, a, img)
- CSS 선택자 (class, id, 속성)
- 크롬 개발자 도구 활용법 (Elements, Network)
- HTTP 요청/응답 구조 이해
- 실습: 원하는 웹 페이지 구조 분석

---

## 세부 일정

| 시간 | 내용 |
|------|------|
| 09:00 – 10:30 | 이론 강의 1 – 개념 설명 및 예제 |
| 10:30 – 10:45 | 휴식 |
| 10:45 – 12:00 | 이론 강의 2 – 심화 개념 |
| 12:00 – 13:00 | 점심 |
| 13:00 – 14:30 | 실습 1 – 코드 따라하기 |
| 14:30 – 14:45 | 휴식 |
| 14:45 – 16:30 | 실습 2 – 응용 문제 풀기 |
| 16:30 – 17:00 | 복습 및 Q&A |

---

## 학습 내용

### 1. HTML 태그 기초 (div, span, table, a, img)

**쉽게 이해하기**
- **HTML 태그 기초 (div, span, table, a, img)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# HTML 태그 기초 (div, span, table, a, img) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. CSS 선택자 (class, id, 속성)

**쉽게 이해하기**
- **CSS 선택자 (class, id, 속성)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# CSS 선택자 (class, id, 속성) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 크롬 개발자 도구 활용법 (Elements, Network)

**쉽게 이해하기**
- **크롬 개발자 도구 활용법 (Elements, Network)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 크롬 개발자 도구 활용법 (Elements, Network) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. HTTP 요청/응답 구조 이해

**쉽게 이해하기**
- **HTTP 요청/응답 구조 이해** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# HTTP 요청/응답 구조 이해 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 실습: 원하는 웹 페이지 구조 분석

**쉽게 이해하기**
- **실습: 원하는 웹 페이지 구조 분석** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: 원하는 웹 페이지 구조 분석 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 실습 과제

오늘 학습한 내용을 바탕으로 아래 과제를 완성하세요.

```python
# Day 024 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

---

## 참고 자료

- 강의 슬라이드: `slides/day024.pdf`
- 실습 노트북: `notebooks/day024.ipynb`

---

## 다음 학습 예고

➡️ [Day 025](day025.md) 에서 계속됩니다.
