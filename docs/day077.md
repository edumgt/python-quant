# Day 077 — Risk-Parity 포트폴리오 구현

> **모듈 11: 나만의 로보 어드바이저 개발 및 성과 검증 프로젝트** | 6/13일차 | 🤖 | 학습시간: 8시간



---

## 오늘 배울 것 (아주 쉽게)

- Risk-Parity 원리 (동등 리스크 기여)
- 리스크 기여도 계산
- Risk-Parity 최적화 구현
- 실습: Risk-Parity vs 평균분산 성과 비교

---



### 1. Risk-Parity 원리 (동등 리스크 기여)

**쉽게 이해하기**
- **Risk-Parity 원리 (동등 리스크 기여)** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# Risk-Parity 원리 (동등 리스크 기여) 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 2. 리스크 기여도 계산

**쉽게 이해하기**
- **리스크 기여도 계산** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# 리스크 기여도 계산 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 3. Risk-Parity 최적화 구현

**쉽게 이해하기**
- **Risk-Parity 최적화 구현** 개념은 `자산 비중 정하기 → 성과 계산 → 비중 조정` 흐름으로 보면 쉽습니다.
- 한 번에 복잡하게 최적화하지 말고 2개 자산 예제로 먼저 감을 잡으세요.

**미니 예시**
```python
# Risk-Parity 최적화 구현 미니 예시
weights = {'stock': 0.6, 'bond': 0.4}
asset_return = {'stock': 0.12, 'bond': 0.04}
portfolio_return = sum(weights[a] * asset_return[a] for a in weights)
print(f'기대수익률: {portfolio_return:.2%}')
```

### 4. 실습: Risk-Parity vs 평균분산 성과 비교

**쉽게 이해하기**
- **실습: Risk-Parity vs 평균분산 성과 비교** 개념은 `일별 수익률 계산 → 누적 성과 확인 → 리스크 지표 비교`로 이해하면 쉽습니다.
- 먼저 작은 구간에서 계산이 맞는지 검증한 뒤, 전체 기간으로 확장하세요.

**미니 예시**
```python
# 실습: Risk-Parity vs 평균분산 성과 비교 미니 예시
returns = [0.01, -0.02, 0.015, 0.005]
cum = 1.0
for r in returns:
    cum *= (1 + r)
print(f'누적수익률: {(cum - 1):.2%}')
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 077 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 078](day078.md) 에서 계속됩니다.
