# Day 013 — 통계 검정

> **모듈 3: AI 및 퀀트 모델링을 위한 기초 수학-통계** | 3/5일차 | 📐 | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 통계 검정 가설검정 파이썬](https://www.youtube.com/results?search_query=통계검정+가설검정+t검정+파이썬+한국어)

## 오늘 배울 것 (아주 쉽게)

- 가설 검정 개념 (귀무가설, 대립가설)
- 유의수준(alpha)과 p-value 해석
- t-검정 (단일표본, 독립표본, 대응표본)
- 카이제곱 검정
- 실습: SciPy를 활용한 주가 데이터 통계 검정

---



### 1. 가설 검정 개념 (귀무가설, 대립가설)

**쉽게 이해하기**
- **가설 검정 개념 (귀무가설, 대립가설)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 가설 검정 개념 (귀무가설, 대립가설) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 유의수준(alpha)과 p-value 해석

**쉽게 이해하기**
- **유의수준(alpha)과 p-value 해석** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 유의수준(alpha)과 p-value 해석 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. t-검정 (단일표본, 독립표본, 대응표본)

**쉽게 이해하기**
- **t-검정 (단일표본, 독립표본, 대응표본)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# t-검정 (단일표본, 독립표본, 대응표본) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 카이제곱 검정

**쉽게 이해하기**
- **카이제곱 검정** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 카이제곱 검정 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 5. 실습: SciPy를 활용한 주가 데이터 통계 검정

**쉽게 이해하기**
- **실습: SciPy를 활용한 주가 데이터 통계 검정** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 실습: SciPy를 활용한 주가 데이터 통계 검정 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 013 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 014](06.md) 에서 계속됩니다.
