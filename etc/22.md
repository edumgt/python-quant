# Day 036 — CNN (Convolutional Neural Network)

> **모듈 6: 퀀트를 위한 머신러닝과 딥러닝** | 8/13일차 | 🤖 | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 CNN 합성곱 신경망 파이썬 딥러닝](https://www.youtube.com/results?search_query=CNN+합성곱+신경망+파이썬+딥러닝+한국어)

## 오늘 배울 것 (아주 쉽게)

- CNN 구조 (합성곱 층, 풀링 층)
- 이미지 데이터 처리 원리
- 1D CNN을 활용한 시계열 패턴 인식
- 차트 이미지 분류 응용
- 실습: 1D CNN으로 주가 패턴 분류

---



### 1. CNN 구조 (합성곱 층, 풀링 층)

**쉽게 이해하기**
- **CNN 구조 (합성곱 층, 풀링 층)** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# CNN 구조 (합성곱 층, 풀링 층) 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 2. 이미지 데이터 처리 원리

**쉽게 이해하기**
- **이미지 데이터 처리 원리** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 이미지 데이터 처리 원리 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 3. 1D CNN을 활용한 시계열 패턴 인식

**쉽게 이해하기**
- **1D CNN을 활용한 시계열 패턴 인식** 개념은 `입력값 확인 → 규칙 적용 → 결과 해석` 순서로 단계별로 보면 쉽습니다.
- 먼저 작은 데이터로 검증한 뒤 실제 데이터에 적용하세요.

**미니 예시**
```python
# 1D CNN을 활용한 시계열 패턴 인식 미니 예시
values = [100, 103, 101, 106]
delta = values[-1] - values[0]
print('시작 대비 변화:', delta)
```

### 4. 차트 이미지 분류 응용

**쉽게 이해하기**
- **차트 이미지 분류 응용** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# 차트 이미지 분류 응용 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

### 5. 실습: 1D CNN으로 주가 패턴 분류

**쉽게 이해하기**
- **실습: 1D CNN으로 주가 패턴 분류** 개념은 `특징 만들기 → 규칙 학습 → 라벨 예측` 3단계로 보면 쉽습니다.
- 모델 성능은 정확도 하나만 보지 말고 오분류 사례를 같이 점검하세요.

**미니 예시**
```python
# 실습: 1D CNN으로 주가 패턴 분류 미니 예시
scores = [0.2, 0.7, 0.9, 0.4]
labels = ['매도' if s < 0.5 else '매수' for s in scores]
print('분류 결과:', labels)
```

---

## 오늘 내용 한눈에 정리

- **CNN**은 그림이나 차트에서 중요한 모양을 자동으로 찾는 딥러닝 모델입니다.
- **합성곱 층**은 작은 창으로 데이터를 훑어 특징(패턴)을 잡아냅니다.
- **풀링 층**은 중요한 정보만 남겨 계산을 가볍게 합니다.
- **1D CNN**은 시간 순서 데이터(주가 같은 시계열)에도 사용할 수 있습니다.

## 용어 사전 (초등학생도 이해하기 쉽게)

| 용어 | 한자 | 영어 | 쉬운 설명 |
|---|---|---|---|
| 딥러닝 | 深層學習 | Deep Learning | 사람 뇌처럼 여러 층으로 배우는 AI |
| 신경망 | 神經網 | Neural Network | 뉴런처럼 연결된 계산 그물 |
| 합성곱 | 卷積 | Convolution | 작은 칸으로 훑으며 특징 찾기 |
| 풀링 | 池化 | Pooling | 중요한 값만 뽑아 줄이는 작업 |
| 패턴 | 樣式 | Pattern | 반복해서 보이는 모양이나 규칙 |
| 시계열 | 時系列 | Time Series | 시간 순서대로 모은 데이터 |

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 036 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


## 다음 시간 미리보기

➡️ [Day 037](23.md) 에서 계속됩니다.


---

<!-- merged from Chapter05.md -->

# Chapter 05. OpenCV로 영상 생성 흐름 이해

> 💡 **쉽게 이해하기**: 플립북(flipbook) 애니메이션처럼, 이미지 한 장 한 장을 쌓아 동영상을 만드는 원리입니다. `VideoWriter`로 코덱·프레임레이트·해상도를 설정하고, 반복문으로 프레임을 추가하면 mp4가 완성됩니다.

---

`OpenCVCPU.py`는 이미지 프레임을 쌓아 mp4를 만드는 가장 기본 패턴입니다.

### 프레임 기반 사고
- 한 장의 이미지 = 한 프레임
- 프레임을 시간 순으로 합치면 비디오

### 코드 포인트
- `VideoWriter`: 코덱/프레임레이트/해상도 지정
- `cv2.circle`: 각 프레임에 원을 그림
- 반복문에서 반지름을 변화시켜 애니메이션 구현

### API화 이점
- 사용자 입력(너비, 높이, FPS)으로 동영상 동적 생성
- 생성 파일을 URL로 반환하여 브라우저에서 바로 재생

---

## 📺 참고 유튜브 영상

| 기술 스택 | 채널 | 링크 |
|---------|------|------|
| OpenCV Python 기초 | freeCodeCamp | [OpenCV Course - Full Tutorial with Python](https://www.youtube.com/watch?v=oXlwWbU8l2o) |
| VideoWriter / 영상 처리 | sentdex | [Python plays GTA V (OpenCV series)](https://www.youtube.com/watch?v=ks4MPfMq8aQ) |
| Computer Vision 입문 | MIT OpenCourseWare | [Computer Vision](https://www.youtube.com/watch?v=715uLCHt4jE) |


---

<!-- merged from Chapter17.md -->

# Chapter 17. 1D CNN — 시계열 주가 패턴 분류

> 💡 **쉽게 이해하기**: 주가 차트 20일치를 보고 "다음 날 오를까, 내릴까, 옆으로 갈까?"를 판단하는 것은 패턴 인식입니다. 1D CNN은 이미지에서 가장자리를 찾듯, 시계열에서 상승·하락 패턴을 자동으로 학습합니다. `CNNTimeSeries.py`로 직접 확인해 보세요.

---

## 📌 이 챕터에서 배우는 것

| 개념 | 설명 |
|------|------|
| 1D 합성곱(Conv1d) | 시간축 방향으로 슬라이딩하며 패턴 추출 |
| 풀링(Pooling) | 특징 압축 — Global Average Pooling으로 고정 크기 출력 |
| 분류 헤드 | FC → Softmax(3클래스: 상승/횡보/하락) |
| 과적합 방지 | Dropout, 교차 검증 |

> **Day036 연결** — `docs/22.md` 이론과 `src/CNNTimeSeries.py` 실습을 함께 진행하세요.

---

## 🔧 실습 파일 실행

```bash
python CNNTimeSeries.py
# 출력: cnn_timeseries.png (학습 손실·정확도 곡선)
```

또는 FastAPI 엔드포인트 호출:

```bash
curl -X POST http://localhost:8000/api/dl/cnn-timeseries \
  -H "Content-Type: application/json" \
  -d '{"window": 20, "n_samples": 2000, "epochs": 20}'
```

---

## 🧱 모델 구조 (요약)

```
입력: (batch, 1, 20)     ← 20일 주가 창 (채널 1개)
  │
  ▼ Conv1d(1→32, kernel=3) + ReLU
  ▼ Conv1d(32→64, kernel=3) + ReLU
  ▼ AdaptiveAvgPool1d(1)   ← Global Average Pooling
  ▼ Flatten → Linear(64→32) + ReLU + Dropout(0.3)
  ▼ Linear(32→3)
출력: (batch, 3)          ← [하락, 횡보, 상승] 확률
```

---

## 📊 학습 결과 해석

| 지표 | 의미 |
|------|------|
| Train Loss↓ / Val Loss↓ | 정상 학습 |
| Val Acc > 0.38 | 3-클래스 랜덤 기대값(0.33)을 상회하면 패턴 학습 중 |
| Val Acc >> Train Acc | 데이터 부족 또는 모델 구조 검토 필요 |

---

## 🔗 퀀트 연결

- **실무 확장**: OHLCV 5채널 입력(`Conv1d(in_channels=5, ...)`)으로 더 풍부한 패턴 학습 가능
- **백테스팅**: 상승 신호 → 매수, 하락 신호 → 숏/현금 보유 전략으로 연결
- **앙상블**: CNN 신호 + RSI/MACD 결합으로 False Positive 감소

---

## 📺 참고 자료

| 주제 | 링크 |
|------|------|
| 1D CNN 시계열 | [Keras: Timeseries classification from scratch](https://keras.io/examples/timeseries/timeseries_classification_from_scratch/) |
| PyTorch Conv1d | [PyTorch 공식 문서](https://pytorch.org/docs/stable/generated/torch.nn.Conv1d.html) |

---

## 📺 YouTube 강의 검색

> [🎬 1D CNN 시계열 주가 패턴 분류 관련 한국어 강의 검색](https://www.youtube.com/results?search_query=1D+CNN+시계열+주가+패턴+분류+파이썬+딥러닝+한국어)
